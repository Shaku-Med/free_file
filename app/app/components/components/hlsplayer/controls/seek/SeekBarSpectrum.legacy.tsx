/**
 * MIGHT NEED LATER — DO NOT TOUCH.
 *
 * Archive slot for the legacy canvas + AnalyserNode seek-bar visualizer
 * (bars / mirror / ribbon / pulse bands). Not imported anywhere.
 *
 * Replaced by StemResonanceVisualizer + audio_stems.json. The live export stub is
 * ./SeekBarSpectrum.tsx (returns null).
 *
 * If you need to restore the old canvas renderer, recover it from version control
 * history for SeekBarSpectrum.tsx before the stem visualizer migration.
 */

export {};
