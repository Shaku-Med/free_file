; This project runs React 19 while some deps (e.g. recharts) still declare a
; React 18 peer range. Use legacy peer resolution so installs are deterministic
; across local + CI/deploy and don't fail with ERESOLVE on upgrades.
legacy-peer-deps=true
